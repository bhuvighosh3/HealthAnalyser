# ──────────────────────────────────────────────────────────────────────────────
# AthleteIQ – Environment Variables
# Copy this file to .env and fill in your values.
# ──────────────────────────────────────────────────────────────────────────────

# ── Strava OAuth (Bhuvi's account / primary) ───────────────────────────────────
STRAVA_CLIENT_ID=223284
STRAVA_CLIENT_SECRET=75c0bd7f853bcdd61a3a7c3320bf759ae451bff6
STRAVA_ACCESS_TOKEN=7358e42b9f2f85f58c28403b9a077d3dc502871d
STRAVA_REFRESH_TOKEN=17fcf13de351679995c16f1576effaa8e069617c
STRAVA_EXPIRES_AT=1776207421

# ── Strava OAuth (Rishit's sample account) ─────────────────────────────────────
RISHIT_CLIENT_ID=223284
RISHIT_CLIENT_SECRET=75c0bd7f853bcdd61a3a7c3320bf759ae451bff6
RISHIT_ACCESS_TOKEN=7358e42b9f2f85f58c28403b9a077d3dc502871d
RISHIT_REFRESH_TOKEN=17fcf13de351679995c16f1576effaa8e069617c
RISHIT_EXPIRES_AT=1776207421

# ── Google Vertex AI ───────────────────────────────────────────────────────────
# All AI features (analysis, forecasting, nutrition, calendar scheduling) run
# through Vertex AI using your college GCP credits — no other AI key needed.
GCP_PROJECT_ID=healthmonitor-493021
GCP_LOCATION=us-central1

# ── Google Calendar MCP (via @cocal/google-calendar-mcp) ──────────────────────
# This lets the AI read your calendar and schedule workout events.
#
# ONE-TIME SETUP (only needed once per machine):
#   1. Go to https://console.cloud.google.com
#   2. Select your GCP project (same as GCP_PROJECT_ID above)
#   3. Enable the Google Calendar API:
#      APIs & Services → Library → search "Google Calendar API" → Enable
#   4. Create OAuth 2.0 credentials:
#      APIs & Services → Credentials → Create Credentials → OAuth client ID
#      Application type: Desktop app  →  Download the JSON
#   5. Save the downloaded JSON somewhere safe, e.g.:
#      /home/yourname/gcp-oauth.keys.json
#   6. Set the path below:
GOOGLE_OAUTH_CREDENTIALS=/path/to/your/gcp-oauth.keys.json
#   7. Authenticate once (opens a browser):
#      npx @cocal/google-calendar-mcp auth
#   8. Restart the server — done!

# ── Firecrawl (for Nutrition RAG feature) ─────────────────────────────────────
# Get your key at: https://firecrawl.dev
FIRECRAWL_API_KEY=your_firecrawl_api_key_here

# ── Server ─────────────────────────────────────────────────────────────────────
PORT=3000
